{
  "type": "object",
  "properties": {
    "topic": {
      "type": "string",
      "description": "Free-text topic, e.g. 'swift 6 strict concurrency' or 'ios 18 control center'."
    },
    "sources": {
      "type": "array",
      "items": {"type": "string"},
      "description": "Optional allow-list of adapter names. Default: all adapters."
    },
    "max_results": {
      "type": "integer",
      "minimum": 1,
      "maximum": 50,
      "default": 8
    },
    "force_refresh": {
      "type": "boolean",
      "default": false
    },
    "rag_question": {
      "type": "string",
      "description": "Optional follow-up question for offline RAG over the cached corpus."
    }
  },
  "required": ["topic"]
}
